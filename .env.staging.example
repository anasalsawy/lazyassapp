# Staging environment (OSS is now the default)
# AUTOBUY_PROVIDER controls where Auto-Shop tasks execute.
# - "oss" (default): Uses local OSS runner at VITE_OSS_RUNNER_URL (no Browser Use credits needed)
# - "cloud": Uses Browser Use Cloud API (requires credits)
# 
# To switch to Cloud, explicitly set: VITE_AUTOBUY_PROVIDER=cloud
AUTOBUY_PROVIDER=oss
VITE_AUTOBUY_PROVIDER=oss
VITE_OSS_RUNNER_URL=http://localhost:8081
