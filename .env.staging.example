# Staging environment (safe defaults)
# AUTOBUY_PROVIDER is the feature flag; Vite reads the VITE_ prefixed variant.
AUTOBUY_PROVIDER=cloud
VITE_AUTOBUY_PROVIDER=cloud
VITE_OSS_RUNNER_URL=http://localhost:8081
