services:
  - type: web
    name: browser-use-bridge
    env: docker
    branch: main
    dockerfilePath: ./Dockerfile
    healthCheckPath: "/health"
    plan: starter
    envVars:
      - key: BRIDGE_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
